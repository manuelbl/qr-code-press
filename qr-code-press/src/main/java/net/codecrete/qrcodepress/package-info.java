/**
 * Generation of QR codes from text and binary data.
 * <p>
 * QR codes are created with {@code QrCode}, either through its static factory methods for the
 * common cases or through its builder for the full parameter set. The resulting code exposes its
 * modules directly and can be rendered as a list of rectangles, an SVG document, an SVG/XAML
 * graphics path or a PNG image.
 * </p>
 */
package net.codecrete.qrcodepress;
